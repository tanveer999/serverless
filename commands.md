serverless or sls:

1. to create serverless
    ```
    serverless
    ```

2. to deploy 
    ```
    sls deploy
    ```
    Arguments:
        --verbose - for detailed log

3. to install plugin
    ```
    sls plugin install -n <plugin name>
